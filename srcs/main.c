#include "fillit.h"

int main(int ac, char **av)
{
  av = NULL;
  if (ac != 2)
    {
      ft_putstr("Usage : ./fillit [arg1]\n        only one argument after bin.\n");
      return (-1);
    }
    return (0);
}
